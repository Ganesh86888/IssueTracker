Simple steps to work with react project

1). Clone this repository in your local (https://github.com/Ganesh86888/IssueTracker.git)

2). Run Command: npm install

3). Start the json serve by running the below command
    (npx json-server -p 3001 -w issue.json )

4). Run Command: npm start
